## Problem

_To be written._

## Architecture

_To be written._

## Results

_To be written._

## Setup

Copy `.env.example` to `.env` and fill in the Postgres connection values (`PGHOST`, `PGPORT`, `PGDATABASE`, `PGUSER`, `PGPASSWORD`); load it with `set -a; source .env; set +a` before running the scripts.

## How to run

_To be written._

## Built with a local AI stack

_To be written._
